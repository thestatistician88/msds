% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/UserFunctions.R
\name{plot_praxes}
\alias{plot_praxes}
\title{Visualize Principal Axes}
\usage{
plot_praxes(
  data,
  axis_label_size = 12,
  point_size = 3,
  main = "",
  xlab = NULL,
  ylab = NULL,
  arrow_scale = NULL
)
}
\arguments{
\item{data}{A data frame with exactly two numeric columns (x and y)}

\item{axis_label_size}{Numeric value controlling the font size of axis labels (default = 12)}

\item{point_size}{Numeric value controlling the size of data points (default = 3)}

\item{main}{Character string for the title (default is empty)}

\item{xlab}{Character string for the x axis variable (default is variable name)}

\item{ylab}{Character string for the y axis variable (default is variable)}

\item{arrow_scale}{Numeric value controlling the length of PC arrows.
If NULL (default), arrows are scaled by 2.5 times the square root of the eigenvalues of the sample covariance amtrix.
If numeric, uses the same scale factor for both PC arrows.}
}
\value{
A list containing:
\item{plot}{ggplot2 object of the visualization}
\item{pca_result}{prcomp object with PCA results}
\item{variance_explained}{Vector of variance explained when converting the data to principal components.}
}
\description{
This function plots the two dimensional data matrix \code{x} along with an estimate of the principal axes obtained via
spectral decomposition of the data's covariance matrix.
}
\examples{
# Create sample data
set.seed(123)
sample_data <- data.frame(x = rnorm(50, 5, 2), y = rnorm(50, 10, 5))
sample_data$y <- sample_data$x + rnorm(50, 0, 1)

# Run PCA visualization with automatic arrow scaling
result <- plot.praxes(sample_data, axis_label_size = 14)
print(result$plot)

# Run with custom arrow scale
result <- plot.praxes(sample_data, arrow_scale = 5)
print(result$plot)

}
